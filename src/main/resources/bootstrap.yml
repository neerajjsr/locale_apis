spring:
  application:
    name: locale-services-api

---
spring:
  profiles: local
  cloud:
    config:
      uri: http://localhost:9012
      name: locale-services-api

---
spring:
  profiles: dev
  cloud:
    config:
      uri:
      name: locale-services-api

---
spring:
  profiles: prod
  cloud:
    config:
      uri:
      name: locale-services-api